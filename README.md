# graph-search-visualizer
A Python Graph Searching system with GUI and visualization supporting BFS, DFS, IDS, UCS, Greedy, and A* search. Users can input nodes, weighted edges, and heuristics (for informed search). Outputs the found path, total cost, and highlights the chosen route.
